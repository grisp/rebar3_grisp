{deps, [grisp]}.

{erl_opts, [debug_info]}.

{grisp, [
    {otp_release, "{{otp_release}}"},
    {deploy, [
        {destination, "{{dest}}"}
    ]}
]}.

{shell, [{apps, []}]}.

{relx, [
    {release, {{{name}}, "0.1.0"}, [{{name}}]}
]}.
